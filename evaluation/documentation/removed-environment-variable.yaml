name: removed-environment-variable
index:
  names: [create_app, Renderer, Renderer.render, MAX_LINES]
  signatures:
    create_app: [config, worker]
    Renderer.render: [version]
  options: ["--strict"]
  environment: [CI_PROJECT_ID]
changes:
  - path: settings.py
    diff: |
      @@ -1,2 +1,1 @@
      -    mode = os.environ["REVIEW_LEGACY_MODE"]
documents:
  docs/config.md: |
    # Configuration

    Set `REVIEW_LEGACY_MODE` to opt in. `CI_PROJECT_ID` comes from the runner.
expected:
  - rule: DOCS.UNKNOWN_OPTION
    line: 3
