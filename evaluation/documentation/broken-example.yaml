# An edited document whose fenced Python does not parse.
name: broken-example
index:
  names: [create_app, Renderer, Renderer.render, MAX_LINES]
  signatures:
    create_app: [config, worker]
    Renderer.render: [version]
  options: ["--strict"]
  environment: [CI_PROJECT_ID]
changes:
  - path: README.md
    diff: |
      @@ -1,3 +1,5 @@
      +```python
      +def (:
      +```
documents:
  README.md: |
    # Guide

    ```python
    def (:
    ```
expected:
  - rule: DOCS.BROKEN_EXAMPLE
    line: 3
